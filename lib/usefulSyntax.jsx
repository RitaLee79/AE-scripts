app.beginUndoGroup("Promo Version Maker") ;

fileContents.split(/\n|\r/);

app.project.item(j).name.match(/_Template$/) ) //el $ al final de RegExp es match del final hacia el principio del string

if (templateComp instanceof CompItem)

for (var h in itemsObject) {

for (k=1; k <= dupComp.numLayers; k++) {

	//TRIM spaces at start and end
	if (String.prototype.trim == null) String.prototype.trim = function () {
		return this.replace(/^\s+/,'').replace(/\s+$/,'');
	};

//TRIM spaces, new lines  etc
if (String.prototype.trim == null) String.prototype.trim = function () {
	return this.replace(/(^[\s\n\r\t\x0B]+)|([\s\n\r\t\x0B]+$)/g, '')
};
